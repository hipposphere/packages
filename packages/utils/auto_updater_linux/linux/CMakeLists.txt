cmake_minimum_required(VERSION 3.13)
set(PROJECT_NAME "auto_updater_linux")
project(${PROJECT_NAME} LANGUAGES CXX)

set(PLUGIN_NAME "${PROJECT_NAME}_plugin")
set(HELPER_NAME "${PROJECT_NAME}_helper")

add_library(${PLUGIN_NAME} SHARED
  "auto_updater_linux_plugin.cc"
)
apply_standard_settings(${PLUGIN_NAME})
set_target_properties(${PLUGIN_NAME} PROPERTIES
  CXX_VISIBILITY_PRESET hidden
)
target_compile_definitions(${PLUGIN_NAME} PRIVATE FLUTTER_PLUGIN_IMPL)
target_include_directories(${PLUGIN_NAME} INTERFACE
  "${CMAKE_CURRENT_SOURCE_DIR}/include"
)
target_link_libraries(${PLUGIN_NAME} PRIVATE flutter PkgConfig::GTK dl)

add_executable(${HELPER_NAME}
  "auto_updater_linux_helper.cc"
)
apply_standard_settings(${HELPER_NAME})

install(TARGETS ${HELPER_NAME}
  RUNTIME DESTINATION "lib"
  COMPONENT Runtime
)
