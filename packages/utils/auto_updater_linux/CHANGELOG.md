## 1.0.1

* Show progress during explicit update checks and surface the final result.
* Accept generic Linux appcast enclosures and Flutter `version+build` version strings.

## 1.0.0

* Add signed AppImage updates for Linux x86_64 and ARM64.
* Add shared Sparkle appcast parsing, GTK update UI, and atomic relaunching.
