# auto_updater_linux

Linux AppImage implementation of `auto_updater`.

Applications should depend on `auto_updater`, which selects this package
automatically on Linux. See the main package README for appcast, signing, and
AppImage packaging requirements.
