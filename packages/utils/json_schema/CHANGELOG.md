## 0.1.0

- Extract typed JSON Schema contracts, registries, references, and the portable
  `@FromSchema` annotation from Dart Edge.
- Own the shared `JsonEncodable` contract and implement it on `JsonSchema`.
