## Next

- Generate finite token-to-`const IconData` switches for Flutter icon preview
  options so release builds can tree-shake icon fonts.

## 0.1.3

- Added generated Flutter Preview bridge functions for widget classes annotated with
  `HippoWidgetPreviewFlutter`.
- Fixed generated catalog names for Flutter preview annotations.
- Added no-argument top-level preview function support in generated catalog builders.

## 0.1.1

- Added generated preview ids and JSON catalog manifest output.
