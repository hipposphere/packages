name: hippo_cli_build
description: Build support package for the Hipposphere CLI.
version: 0.1.0
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org/pub/internal-pub/"
resolution: workspace

environment:
  sdk: ">=3.12.0 < 4.0.0"

dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0

dependencies:
  hippo_cli_core:
    hosted: https://pub.hippolabs.org/pub/internal-pub/
    version: ^0.1.0
  path: ^1.9.1
  yaml: ^3.1.3
