name: hippo_cli
description: Command line interface package for Hipposphere tooling.
version: 0.1.0
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org/pub/internal-pub/"
resolution: workspace

executables:
  hippo:

environment:
  sdk: ">=3.12.0 < 4.0.0"

dependencies:
  args: ^2.7.0
  hippo_cli_build:
    hosted: https://pub.hippolabs.org/pub/internal-pub/
    version: ^0.1.0
  hippo_cli_core:
    hosted: https://pub.hippolabs.org/pub/internal-pub/
    version: ^0.1.0
  hippo_cli_skills:
    hosted: https://pub.hippolabs.org/pub/internal-pub/
    version: ^0.1.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0
