## 0.1.0

- Add typed host, CPU, memory, swap, load, disk, mount, network, and process inspection.
- Add counter-based CPU and throughput sampling.
- Add narrow libc bindings for filesystem capacity and system constants.
