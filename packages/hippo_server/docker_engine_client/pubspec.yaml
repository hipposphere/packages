name: docker_engine_client
description: Typed Dart client for the Docker Engine HTTP API over Unix sockets or TCP/TLS.
version: 0.1.0
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org"
resolution: workspace

environment:
  sdk: ">=3.13.0 <4.0.0"

dev_dependencies:
  test: ^1.30.0
