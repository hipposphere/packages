name: docker_compose_client
description: Typed, shell-free Dart client for Docker Compose project operations.
version: 0.2.0
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org"
resolution: workspace

environment:
  sdk: ">=3.13.0 <4.0.0"

dev_dependencies:
  test: ^1.30.0
