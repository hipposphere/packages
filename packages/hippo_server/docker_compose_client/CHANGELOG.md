## 0.2.0

- Support ordered, repeated Docker Compose `--env-file` arguments.

## 0.1.0

- Add typed validation, pull, up, down, start, stop, restart, status, and bounded
  log operations for Docker Compose projects.
- Add injectable shell-free process execution with deadlines and structured
  errors.
