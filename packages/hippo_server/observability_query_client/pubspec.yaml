name: observability_query_client
description: Read-only Dart clients for Prometheus, Loki, and Grafana Tempo query APIs.
version: 0.1.0
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org"
resolution: workspace

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  http: ^1.6.0

dev_dependencies:
  test: ^1.30.0
