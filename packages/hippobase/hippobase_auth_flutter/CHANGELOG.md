## 0.1.1

- Require Dart Edge Core 0.3.34 and `hippobase_auth_client` 0.1.1.

## 0.1.0

- Add provider, reactive builder, and state-routing view backed by
  `hippobase_auth_client`.
- Re-export the client bindings without duplicating API or session models.
