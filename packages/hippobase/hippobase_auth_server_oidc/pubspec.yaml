name: hippobase_auth_server_oidc
description: OpenID Connect adapter for Hippobase Auth servers.
version: 0.1.1
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org"
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  hippobase_auth_server_engine:
    hosted: "https://pub.hippolabs.org"
    version: ^0.1.1
  openid_client: ^0.4.9+1

dev_dependencies:
  test: ^1.30.0
