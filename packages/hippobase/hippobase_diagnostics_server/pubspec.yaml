name: hippobase_diagnostics_server
description: Server and Dart Edge logging adapters for Hippobase diagnostics.
version: 0.1.1
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org"
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  dart_edge_observability:
    hosted: "https://registry.hippolabs.org/pub/internal-pub/"
    version: ^0.1.1
  hippobase_diagnostics:
    hosted: "https://pub.hippolabs.org"
    version: ^0.1.0

dev_dependencies:
  test: ^1.30.0
