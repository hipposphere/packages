# hippobase_diagnostics_server

Server and Dart Edge adapters for `hippobase_diagnostics`.

The package lets a Dart Edge service tee `JsonLogger` events into a local diagnostics store while
preserving the existing stdout/Loki logger.
