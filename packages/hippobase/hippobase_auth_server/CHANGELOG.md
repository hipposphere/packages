## 0.1.1

* Require Dart Edge Core 0.3.34, HTTP Server 0.3.18, and the updated
  Hippobase auth and core packages.

## 0.1.0

* Add the HTTP routing façade for the independently packaged auth engine, SQL
  persistence, and OIDC adapter.
* Add resource-oriented admin user routes with typed Hippobase pagination.
