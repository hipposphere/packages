## 0.1.0

* Add schema-backed pagination, sorting, filtering, and paginated response models.
* Add snake-case JSON schema fields and filter operator wire values.
* Add reusable JSON schema exports for project registries.
