name: hippobase_core_models
description: Shared Hippobase pagination, sorting, and filtering query models.
version: 0.1.0
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org"
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  json_schema:
    hosted: "https://pub.hippolabs.org"
    version: ^0.1.1

dev_dependencies:
  build_runner: ^2.15.0
  json_schema_gen:
    hosted: "https://pub.hippolabs.org"
    version: ^0.1.1
  test: ^1.30.0
