name: hippobase_diagnostics
description: Local support diagnostics primitives for Hippobase app and server runtimes.
version: 0.1.0
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org"
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  path: ^1.9.1

dev_dependencies:
  test: ^1.30.0
