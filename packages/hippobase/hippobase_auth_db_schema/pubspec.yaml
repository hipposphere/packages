name: hippobase_auth_db_schema
description: Shared Dart Edge SQL schema for Hippobase Better Auth tables.
version: 0.1.2
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org"
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  dart_edge_sql_migrator:
    hosted: "https://registry.hippolabs.org/pub/internal-pub/"
    version: ^0.3.19

dev_dependencies:
  dart_edge_sql:
    hosted: "https://registry.hippolabs.org/pub/internal-pub/"
    version: 0.3.31
  test: ^1.30.0
