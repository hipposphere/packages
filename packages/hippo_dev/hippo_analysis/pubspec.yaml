name: hippo_analysis
description: Shared analysis and Dart formatting policy for Hipposphere packages.
version: 0.1.2
homepage: https://hippolabs.org
publish_to: "https://pub.hippolabs.org"
resolution: workspace

environment:
  sdk: ">=3.12.0 < 4.0.0"

dependencies:
  dart_style: ^3.1.9
  flutter_lints: ^6.0.0
  lints: ^6.1.0

dev_dependencies:
  test: ^1.30.0
