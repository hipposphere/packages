name: hippo_core
description: Core reactive primitives for Hipposphere Dart packages.
version: 0.1.4
homepage: https://hippolabs.org
publish_to: "https://registry.hippolabs.org/pub/internal-pub"
resolution: workspace

environment:
  sdk: ">=3.12.0 < 4.0.0"

dependencies:
  rxdart: ^0.28.0

dev_dependencies:
  lints: ^6.0.0
