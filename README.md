# Hippo Packages Workspace

Melos workspace for Hipposphere packages.

## Layout

- `packages/`: workspace packages

## Commands

```sh
dart pub get
dart pub global activate melos
melos bootstrap
melos run ci
```

Packages can be released from GitHub Actions with the `Release Packages` workflow.
